# 参与贡献

欢迎通过 Issue 和 Pull Request 改进“我的课表”。

## 开发流程

1. Fork 仓库并创建功能分支。
2. 修改 `Sources/` 中的 Swift 源码。
3. 运行 `./scripts/build-release.sh`，确认两个架构均可编译且签名验证通过。
4. 手动检查课程编辑、周次切换、地图、事项和导出功能。
5. 提交 Pull Request，并说明修改内容和验证方式。

请勿提交真实账号、密码、Cookie、个人课表数据、证书或公证凭据。
